{{- if eq .Values.global.platform "OPENSHIFT" }}
apiVersion: v1
kind: Secret
metadata:
  name: webhook-secret 
  namespace: {{ .Release.Namespace | quote }}
type: Opaque
data:
  WebHookSecretKey: {{ .Values.global.trigger.secret | b64enc }}
{{- end }}